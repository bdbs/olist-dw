-- ============================================================
-- 通用测试：字符串字段不能为空字符串
-- ------------------------------------------------------------
-- 场景：ODS 层常见 '' 空字符串，它不等于 NULL，
--       not_null 测试查不出来，需要专门拦截
-- ============================================================

{% test assert_not_empty_string(model, column_name) %}

SELECT *
FROM {{ model }}
WHERE {{ column_name }} IS NULL
   OR TRIM({{ column_name }}) = ''

{% endtest %}
