-- ============================================================
-- 自定义质量测试：GMV 非负性校验
-- ------------------------------------------------------------
-- 业务规则：成交金额不可能是负数
-- 如果出现负数，说明上游金额计算或数据本身有问题
-- ============================================================

SELECT
    order_month,
    gmv_delivered
FROM {{ ref('ads_gmv_monthly') }}
WHERE gmv_delivered < 0
