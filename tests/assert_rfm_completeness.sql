-- ============================================================
-- 自定义质量测试：RFM 分层完整性校验
-- ------------------------------------------------------------
-- 断言：每个客户都必须被分到某个 RFM 层，不能为 NULL
-- 这是"完整性"维度的检查
-- ============================================================

SELECT
    customer_id,
    rfm_segment
FROM {{ ref('dws_customer_rfm') }}
WHERE rfm_segment IS NULL
   OR rfm_segment = ''
